# Library modules have no targetSdk in their test manifest, so Robolectric would default to the newest
# SDK it ships, ahead of the app's targetSdk (gradle/libs.versions.toml) and of what the Compose test
# rule's Espresso input injection supports. Pin to the app's target instead.
sdk=36
